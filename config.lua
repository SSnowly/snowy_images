return {
    GenerateOnScriptStart = true, -- replicates the images into the ox_inventory/web/images folder on startup.
    ForceDeleteImagesBeforeReplicating =false, -- forces the images in ox_inventory/web/images to be deleted before copying new ones from the images folder.
}
